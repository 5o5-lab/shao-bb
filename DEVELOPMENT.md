# Shao-bb 项目开发与协作指南

为了保证不同开发者与 AI 协作者在维护和扩展 `shao-bb` 时保持一致的产品方向、文档风格与交付标准，特制定本指南。

本文件解决四件事：
- 统一协作规则。
- 统一文档与 Prompt 风格。
- 明确当前已完成范围。
- 用 `DONE / DOING / TODO / KNOWN LIMITATIONS` 管理路线图。

---

## 1. 协作目标

`shao-bb` 不是普通文案项目。它的目标是把中文大模型交互中的冗余 Token 压到最低，同时不牺牲技术准确性与操作安全性。

协作者必须同时守住三条线：
- **省流**：删废话。保留必要信息。
- **准确**：不能因压缩而损失技术含义。
- **安全**：高风险场景必须退出极简模式，改用清晰确认语气。

---

## 2. 统一风格规范

### 2.1 产品哲学
- 目标不是“说得短”，而是“信息密度高”。
- 删除的是客套、情绪糖精、重复解释、空洞过渡。
- 不删除必要约束、参数、边界条件、风险提示。
- 默认输出结构遵循：`问题 -> 结论 -> 动作`。

### 2.2 Prompt 编写规范
- 所有技能入口文件使用 Markdown。
- 文件头必须包含 YAML 元数据。
- `name` 使用 `kebab-case`。
- `description` 必须写清用途、范围、强度或模式差异。
- 指令正文优先使用短句、列表、表格，不写大段角色扮演式废话。
- 任何具备执行倾向的模块都必须带 `Auto-Clarity` 或同等安全降级规则。

### 2.3 文档写作规范
- 面向协作者的文档优先写中文。
- 标题命名直白，不用营销黑话。
- 示例必须可对照，最好包含 `before / after`。
- 如果写效果结论，必须说明它是“示例”“目标”还是“已验证结果”。
- 未验证的能力，禁止写成已支持。

### 2.4 命名规范
- 目录名、技能名、脚本名统一使用 `kebab-case`。
- 通用说明文档使用大写文件名，如 `README.md`、`DEVELOPMENT.md`。
- 示例文件放在 `examples/` 下，文件名直接表达用途，如 `before-after.md`。

---

## 3. 工程协作规范

本项目当前以文档与 Prompt 为主，但从现在开始按“可持续扩展为脚本/插件项目”的标准协作。

### 3.1 修改原则
- 小步提交。一次改动只解决一类问题。
- 不把“文案润色”“能力扩展”“结构重排”混在同一次修改里。
- 改动 `skill.md` 时，同步检查 `README.md`、示例文档、路线图是否过期。
- 如果新增模式、子技能或脚本，必须更新本文件状态区。

### 3.2 提交与评审要求
- 提交信息建议遵循 Conventional Commits。
- PR 或补丁说明至少写清三件事：改了什么、为什么改、如何验证。
- 评审重点不是文风喜好，而是：
  - 是否更省 Token。
  - 是否保持技术准确。
  - 是否引入误导或安全风险。
  - 是否同步更新了文档状态。

### 3.3 验收标准
一个改动要算“完成”，至少满足：
- 文档或 Prompt 自洽，无互相矛盾描述。
- 示例能覆盖改动后的行为。
- 高风险场景仍保留清晰确认机制。
- 若新增脚本或工具，必须补充运行方式与最小输入输出示例。

### 3.4 兼容与回退原则
- 未确认兼容的平台，不写“支持”。
- 改动若影响既有触发词、模式名、示例输出，必须在文档中明确迁移方式。
- 如果某个极简表达可能导致误操作，宁可变长，不可硬省。

### 3.5 AI 协作者约束
- AI 协作者修改本项目时，优先遵守本文件而不是自由发挥。
- 不得擅自引入与项目哲学冲突的“长解释模板”。
- 不得把设想中的功能写成当前已实现。
- 不得删除安全降级规则，除非有更严格替代方案。

---

## 4. 当前结构

```text
f:\skills\shao-bb\
├── skill.md
├── README.md
├── DEVELOPMENT.md
└── examples/
    ├── before-after.md
    └── wenyan-demo.md
```

当前仓库仍是“文档型原型”：
- 已有核心 Skill 定义。
- 已有三档模式说明。
- 已有对比示例与文言示例。
- 尚无脚本、测试、自动分发、CLI 或 IDE 集成实现。

---

## 5. 项目状态面板

### DONE
- `[x]` 定义项目定位：面向中文大模型/Agent 的省流 Skill。
- `[x]` 完成核心技能文档 `skill.md`，包含 `lite / ultra / wenyan` 三档模式。
- `[x]` 建立基础说明文档 `README.md`。
- `[x]` 建立示例文档：
  - `[x]` `examples/before-after.md`
  - `[x]` `examples/wenyan-demo.md`
- `[x]` 建立开发协作指南 `DEVELOPMENT.md`。
- `[x]` 为技能设计安全降级规则：高危操作退出极简模式。

### DOING
- `[ ]` 整理协作规范与路线图结构，使状态面板更适合多人/多 Agent 共同维护。
- `[ ]` 校准文档边界，避免把“已实现能力”和“规划能力”写混。

### TODO

#### 交互接入
- `[ ]` 做自动注入层。
  - 目标：让 `shao-bb` 不只靠手动调用，而是能在支持的平台中自动把核心 system prompt 注入会话。
  - 范围：优先支持一个平台；模式可切换；可安全停用。
- `[ ]` 做模式状态管理。
  - 目标：支持 `lite / ultra / wenyan / 停用` 等模式切换，并让当前状态可感知。
  - 范围：至少要解决“模式切了但系统不知道”这个问题。

#### 安装与分发
- `[ ]` 做最小安装与分发方案。
  - 目标：让别人能快速装上并试用，而不是只看文档手动拼。
  - 范围：优先支持一种平台；同时保留对无 hook 平台的规则文件方案。

#### 子技能与工具
- `[ ]` `shao-bb-commit`
  - 目标：读取变更，生成极简但合规的提交信息。
  - 最低交付：明确输入、输出、提交格式示例。
- `[ ]` `shao-bb-review`
  - 目标：输出一行式代码审查结论。
  - 最低交付：明确行号格式、风险等级、建议模板。
- `[ ]` `shao-bb-compress` CLI
  - 目标：压缩冗长 Markdown 文档为 shao-bb 风格。
  - 最低交付：本地运行方式、备份策略、输入输出示例。

#### 工程化
- `[ ]` 补充安装与验收文档。
- `[ ]` 补充兼容平台清单。
- `[ ]` 补充 benchmark 方法，验证“省流”效果表述。
- `[ ]` 统一入口文件命名与实际使用平台要求。
- `[ ]` 明确首发范围。
  - 原则：先做最小可运行链路，不照抄 `caveman` 的完整仓库形态。

### KNOWN LIMITATIONS
- `[!]` 当前仓库主要是 Prompt 与文档原型，不是可执行产品。
- `[!]` 尚无自动化测试或回归检查。
- `[!]` 尚无真实 benchmark 数据支撑 README 中的效果描述。
- `[!]` 尚未验证不同 Agent 平台对本 Skill 文件格式的兼容性。
- `[!]` 文言模式强调信息密度，但在部分技术场景下可能降低新协作者理解速度。
- `[!]` `caveman` 的仓库结构较完整，但不代表 `shao-bb` 初版必须同步复制 `hooks / benchmarks / evals / commands / tests` 全套形态。
- `[!]` 当前更适合优先保留关键链路：`核心 prompt -> 自动注入/分发 -> 模式切换 -> 安装说明`。

---

## 6. 维护规则

每次修改本项目后，至少检查以下四项：
- `skill.md` 是否仍与 `README.md` 描述一致。
- 示例文档是否仍能证明当前行为。
- `DONE / DOING / TODO / KNOWN LIMITATIONS` 是否需要更新。
- 是否引入了更短但更危险的表达。

如果答案里有一项是否定，改动还不算完成。
