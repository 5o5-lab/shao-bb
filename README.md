<h1 align="center">🪨 Shao-bb</h1>

<p align="center">
  <strong>少BB 中文极简版。能用五个字说清的事，绝不用一段话。</strong>
</p>

---

这是一个专为中文大语言模型及 Agent 设计的省流 (Token-saving) 技能（Skill）。基于海外原版 [Caveman](https://github.com/JuliusBrussee/caveman) 的核心理念二次重构，目标是大幅削减无效 Token 与废话，同时尽量保留完整准确的技术见解与代码逻辑。**核心理念：删掉低价值表达，不删必要信息。**

## 为什么需要 Shao-bb？

看看我们在日常对话中浪费了多少 Token 和生命：

### 🗣️ 普通模式（消耗 60+ Tokens，含大量情绪糖精）

> “好的，我知道你现在的情况了！关于你在 React 中遇到的无限重渲染的问题，主要是因为你在进行属性传递的时候每次都传入了一个全新的内联对象，React 的浅层比较机制会认为其是一个新的引用。所以我建议你使用 useMemo 把它包裹起来哦！”

### 🪨 Shao-bb Ultra 模式（示例输出，刀刀见血）

> “内联对象作 prop。每次渲染生成新引用 -> 触发重渲染。用 `useMemo`。”

---
**目标：高信息密度 | 少废话 | 技术含义不缩水**

## 两大挡位 (Levels)

| 模式 | 指令 | 效果演示 |
|---|---|---|
| **Lite (微省流)** | `/shao-bb lite` | “React 重渲染因为内联对象导致了新的引用。用 `useMemo` 包裹住该对象即可。” |
| **Ultra (电报体)** | `/shao-bb ultra` | “内联对象 -> 新引用 -> 触发重渲染。使用 `useMemo` 包裹（注：确保语义完整）。” |

## 安装与使用

当前首发目标按 Codex Skill 目录规范推进。其他 Agent 平台兼容方案属于后续待办。

- **Codex 入口文件**：`skills/shao-bb/SKILL.md`。
- **Codex 显式激活**：输入 `shao-bb ultra` 或 `shao-bb lite`。
- **通用显式激活**：对话时开头加入 `/shao-bb ultra` 或“电报模式”。
- **恢复原状**：输入“停止省流”或“退出电报体”。

### Codex 首发验收

在 Codex 会话中输入：

```text
shao-bb ultra
```

期望看到类似反馈：

```text
SHAO-BB ultra 已启用。
```

继续输入一个技术问题，回答应更短、更直接，但不能丢失必要命令、路径、错误信息或风险条件。

再输入：

```text
shao-bb lite
```

期望看到类似反馈：

```text
SHAO-BB lite 已启用。
```

Lite 应比 Ultra 更接近自然句，但仍删除客套、铺垫和重复解释。

### 跨会话记忆验证 (State Persistence Check)

由于 Shao-bb 支持状态驻留，当你在对话中要求切换状态（如 `shao-bb lite`）后，可以按以下步骤验证闭环：
1. 请观察工作区硬盘目录下是否成功生成/修改了 `.codex/shao-bb.mode` 文件，且内容为你刚切换的模式名（如 `lite` 或 `off`）。
2. 关闭并重新启动当前 Agent 会话（触发 `SessionStart` 挂载钩子）。
3. 留心观察控制台第一句装载打印，应该输出对应你上一次状态的规则，例如：`SHAO-BB (lite) 已激活...` 而不是被生硬重置回默认的 `ultra`。
这证明基于底层的状态记忆功能已正式跨越会话生效。

### 何时它会自动恢复正常说话？
当系统判定你即将执行**高危操作**（例如删除核心库、覆盖数据库等）时，Shao-bb 必须退出极简模式，切回清晰确认语气。

> *“高危鉴别。暂停省流。请确认是否要覆盖主库表结构...”*

## 开发规划与拓展

未来我们将借鉴原版仓库持续加入以下子模块：
- `shao-bb-commit`：自动生成遵守规范的电报式提交记录。
- `shao-bb-review`：一行直击痛点的 PR / MR 审查结论。
